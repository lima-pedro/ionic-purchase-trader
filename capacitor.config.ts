import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.br.planetatrader.revista',
  appName: 'ionicPurchase',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
